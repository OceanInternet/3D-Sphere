var radius = 400,
    rows   = 16,
    cols   = rows / 2,
    top    = 0,
    col    = 0;

for(bottom=1;bottom<=rows;bottom++) {

    var yAngle = 180 * bottom / rows;

    x = radius * cos(a)
    y = radius * sin(a)
    
    top = bottom;
}
